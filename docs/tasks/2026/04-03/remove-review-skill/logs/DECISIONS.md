# Decisions

**2026-04-03**

- `structure-first-review`는 저장소에서 제거한다. 유지보수 가치보다 노출 복잡도만 남아 있기 때문이다.
- 관련 문서 정리는 외부 노출면으로 한정하고, 내부 운영 문서의 일반 `review` 용어는 그대로 둔다.
- 스킬 메타데이터 `version` 복구는 이번 작업에 함께 포함한다. 현재 워크트리 변경이 그 삭제만 담고 있기 때문이다.
- 이번 변경은 검증 후 하나의 커밋으로 묶는다.
