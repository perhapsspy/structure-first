**2026-03-30**

- `AGENTS.md`, 기존 `project-context` adoption task의 `BRIEF.md`, 저장소 전체 `project-context` 검색 결과를 읽어 현재 canonical source와 중복 문서 위치를 확인했다.
- `docs/reference/` 아래에는 `project-context.md` 한 파일만 남아 있었고, 내용상 스킬 계약을 저장소 안에 복제한 문서로 분류했다.
- `AGENTS.md`에 `project-context` 사용 원칙과 동명 계약 문서 비복제 규칙을 추가했다.
- `docs/reference/project-context.md`를 삭제하고 이번 변경용 날짜별 task 문서를 추가했다.
