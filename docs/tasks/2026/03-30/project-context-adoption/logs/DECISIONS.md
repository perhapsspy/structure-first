# Decisions

**2026-03-30**

- 이 저장소는 `project-context`의 기본 런타임 구조(`docs/reference/`, `docs/tasks/...`)를 채택한다.
- 저장소 고유 규칙만 남기고, 일반 `project-context` 계약 자체는 `AGENTS.md`와 설치된 스킬 문서를 canonical source로 본다.
- dogfood 산출물은 durable 자산과 disposable run output을 분리해 운영한다.
- 외부 노출 문서의 `project-context` 안내도 같은 저장소 내 task/reference 모델로 맞춘다.
