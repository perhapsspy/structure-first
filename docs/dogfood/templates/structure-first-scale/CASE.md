# Case: <case-name>

## Intent

이 케이스가 무엇을 드러내려는지 한 문장으로 적는다.

## Scale

- 검증할 현재 작업 단위: `function/file | module/use case | capability/subsystem`

## Writer Brief

- 언어: `<language>`
- 생성 대상:
  - `before/<file-or-files>`
- 그럴듯한 실제 코드처럼 만들되, 특정 코드 철학에 맞춰 최적화하지 않는다.
- 이 케이스는 아래를 드러내야 한다:
  - `<behavior 1>`
  - `<behavior 2>`
  - `<behavior 3>`

## Applier Goal

- 같은 케이스를 `structure-first`로 개선한다.
- 현재 작업 단위를 `<scale>`로 유지한다.
- 이 케이스의 명시적 anti-goal이 있으면 적는다.

## Reviewer Focus

- `<question 1>`
- `<question 2>`
- `<question 3>`
